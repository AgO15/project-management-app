# STAGING ENVIRONMENT CONFIGURATION
# This file is for your local staging server
# Copy this to .env.staging and fill in your staging credentials

# Google AI API (can use same as production or separate)
GOOGLE_API_KEY=your_google_api_key_here

# Supabase Configuration - STAGING PROJECT
# Option 1: Create a separate Supabase project for staging
# Option 2: Use the same project but with a different database schema
NEXT_PUBLIC_SUPABASE_URL=your_staging_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_staging_supabase_anon_key

# Site Configuration - Staging Server
# Use a different port (3001) to run alongside production (3000)
NEXT_PUBLIC_SITE_URL=http://localhost:3001
NEXT_PUBLIC_DEV_SUPABASE_REDIRECT_URL=http://localhost:3001/auth/callback

# Environment identifier
NODE_ENV=development
NEXT_PUBLIC_APP_ENV=staging
